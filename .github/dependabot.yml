version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    labels:
      - dependencies
      - github-actions

# Copy the relevant ecosystem block into a product repository when it is created.
# Keep update scope small and review every dependency change through pull requests.
